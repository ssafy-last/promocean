// frontend/src/app/contest/[postId]/@modal/default.tsx

/**
 * Default modal slot
 * @description layout.tsx에서 modal이 없을 경우 렌더링되는 모달 슬롯
 * @returns {null}
 */
export default function Default() {
  return null;
}